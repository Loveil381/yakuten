---
title: HRT药典 — 跨性别女性循证用药安全指南（减害 · 引导就医）
description: 面向跨性别女性的 HRT 循证用药安全指南，基于 WPATH SOC 8、Endocrine Society 2017 与同行评审文献，提供从基线血检到长期维持的完整路径、剂量红线、药物比较、紧急症状识别与中国友好医疗资源。
evidenceLevel: X
lastReviewed: 2026-04-07
references: []
template: splash
hero:
  title: |
    <span class="sr-only">HRT药典</span>
  tagline: ""
---

import EmergencyBanner from '../../../components/ui/EmergencyBanner.astro';
import SplashNav from '../../../components/layout/SplashNav.astro';
import ParticleBackground from '../../../components/layout/ParticleBackground.astro';
import ParticleCanvas from '../../../components/interactive/ParticleCanvas.tsx';
import HeroSection from '../../../components/layout/HeroSection.astro';
import ActionCards from '../../../components/layout/ActionCards.astro';
import GuideScenarioStrip from '../../../components/layout/GuideScenarioStrip.astro';
import DrugQuickNav from '../../../components/layout/DrugQuickNav.astro';
import TrustPillars from '../../../components/layout/TrustPillars.astro';
import MissionStatement from '../../../components/layout/MissionStatement.astro';
import SiteFooter from '../../../components/layout/SiteFooter.astro';
import { getSortedPosts, getCategoryLabel, getBlogUrl } from '../../../utils/blogHelpers';
import { getReadingTime } from '../../../utils/readingTime';

export const recentPosts = (await getSortedPosts('zh')).slice(0, 4);

<SplashNav />

<EmergencyBanner />

<ParticleBackground />

<ParticleCanvas client:idle />

<HeroSection />

<ActionCards />

<GuideScenarioStrip />

<DrugQuickNav />

<TrustPillars />

<section class="recent-blog" aria-label="最近博客">
  <div class="recent-blog__head">
    <h2 class="recent-blog__title">最近博客</h2>
    <a class="recent-blog__more" href="/zh/blog/">查看全部 →</a>
  </div>
  <div class="recent-blog__grid">
    {recentPosts.map((post) => {
      const url = getBlogUrl(post);
      const dateStr = post.data.publishDate.toLocaleDateString('zh-CN', {
        year: 'numeric',
        month: 'short',
        day: 'numeric',
      });
      const readingTime = getReadingTime(post.data.description, 'zh');
      return (
        <a href={url} class="recent-blog__card glass-panel">
          <div class="recent-blog__meta">
            <span class="recent-blog__category">{getCategoryLabel(post.data.category, 'zh')}</span>
            <span>{dateStr}</span>
          </div>
          <h3 class="recent-blog__card-title">{post.data.title}</h3>
          <p class="recent-blog__card-desc">{post.data.description}</p>
          <span class="recent-blog__readtime">约 {readingTime} 分钟阅读</span>
        </a>
      );
    })}
  </div>
</section>

<div class="quick-tools">
  <div class="quick-tools__inner">
    <div class="quick-tools__stats">
      覆盖 <strong>20 种药物</strong> · <strong>25 家</strong>友好医院 · 全部引用标注 DOI
    </div>
    <div class="quick-tools__links">
      <a href="/zh/tools/blood-checker/" class="quick-tools__link">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/><path d="M9 15l2 2 4-4"/></svg>
        血检自查
      </a>
      <a href="/zh/tools/hospital-finder/" class="quick-tools__link">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
        查找医院
      </a>
      <a href="/zh/tools/risk-screener/" class="quick-tools__link">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><path d="M9 12l2 2 4-4"/></svg>
        风险自评
      </a>
    </div>
  </div>
</div>

<style>{`
  .recent-blog {
    position: relative;
    z-index: 10;
    padding: var(--space-xl) var(--space-lg);
    max-width: 80rem;
    margin: 0 auto;
  }
  .recent-blog__head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: var(--space-md);
    margin-bottom: var(--space-lg);
    flex-wrap: wrap;
  }
  .recent-blog__title {
    font-family: var(--font-display);
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 0;
    letter-spacing: 0.02em;
  }
  .recent-blog__more {
    font-family: var(--font-mono);
    font-size: 0.8125rem;
    color: var(--color-primary-light);
    text-decoration: none;
    letter-spacing: 0.05em;
    transition: color var(--transition-fast);
  }
  .recent-blog__more:hover {
    color: var(--color-accent);
  }
  .recent-blog__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-md);
  }
  @media (min-width: 640px) {
    .recent-blog__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (min-width: 1024px) {
    .recent-blog__grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }
  .recent-blog__card {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
    padding: var(--space-lg);
    color: inherit;
    text-decoration: none;
    transition: transform var(--transition-normal), border-color var(--transition-normal);
    clip-path: var(--clip-corner);
  }
  .recent-blog__card:hover {
    transform: translateY(-2px);
  }
  @media (prefers-reduced-motion: reduce) {
    .recent-blog__card:hover {
      transform: none;
    }
  }
  .recent-blog__meta {
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-text-muted);
    letter-spacing: 0.05em;
  }
  .recent-blog__category {
    color: var(--color-accent);
    font-weight: 600;
    text-transform: uppercase;
  }
  .recent-blog__card-title {
    font-family: var(--font-display);
    font-size: 1.0625rem;
    font-weight: 700;
    line-height: 1.4;
    color: var(--color-text-primary);
    margin: 0;
  }
  .recent-blog__card-desc {
    font-size: 0.875rem;
    line-height: 1.6;
    color: var(--color-text-muted);
    margin: 0;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .recent-blog__readtime {
    margin-top: auto;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-text-muted);
    letter-spacing: 0.05em;
  }

  .quick-tools {
    position: relative;
    z-index: 10;
    padding: 0 var(--space-lg) var(--space-xl);
    max-width: 80rem;
    margin: 0 auto;
  }
  .quick-tools__inner {
    background: var(--glass-bg);
    backdrop-filter: var(--glass-blur);
    -webkit-backdrop-filter: var(--glass-blur);
    border: var(--glass-border);
    clip-path: var(--clip-corner);
    padding: var(--space-md) var(--space-lg);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-lg);
    flex-wrap: wrap;
  }
  .quick-tools__stats {
    font-family: var(--font-body);
    font-size: 0.8125rem;
    color: var(--color-text-muted);
    letter-spacing: 0.02em;
  }
  .quick-tools__stats strong {
    color: var(--color-accent);
    font-weight: 700;
  }
  .quick-tools__links {
    display: flex;
    gap: var(--space-md);
    flex-wrap: wrap;
  }
  .quick-tools__link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    padding: var(--space-md);
    font-family: var(--font-body);
    font-size: 0.8125rem;
    color: var(--color-primary-light);
    border: 1px solid var(--color-outline-20);
    text-decoration: none;
    transition: border-color var(--transition-fast), background var(--transition-fast);
  }
  .quick-tools__link:hover {
    border-color: var(--color-primary);
    background: rgba(200, 75, 124, 0.08);
  }
  @media (max-width: 640px) {
    .quick-tools__inner {
      flex-direction: column;
      text-align: center;
    }
  }
`}</style>

<MissionStatement />

<SiteFooter />
